/**
 * Algorithm 6: Weighted Network with Travel Times
 * Build weighted adjacency list with travel times
 */

import { WeightedNetwork } from './types';

/**
 * Build weighted network from connections with travel times
 * @param connections - Array of [station1, station2, travelTime]
 * @returns Weighted adjacency list
 */
export function buildWeightedNetwork(
  connections: [string, string, number][]
): WeightedNetwork {
  // TODO: Implement using TDD
  // Each connection should be bidirectional with same weight
  throw new Error('Not implemented yet - write your tests first!');
}

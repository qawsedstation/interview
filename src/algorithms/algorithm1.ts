/**
 * Algorithm 1: Build the Network
 * Convert connections array to adjacency list
 */

import { Network } from './types';

/**
 * Build network adjacency list from connections
 * @param connections - Array of [station1, station2] connections
 * @returns Adjacency list representation
 */
export function buildNetwork(connections: string[][]): Network {
  // TODO: Implement using TDD
  // Remember: Write tests first!
  throw new Error('Not implemented yet - write your tests first!');
}

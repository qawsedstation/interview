/**
 * Algorithm 3: Check if Route Exists
 * Check if two stations are directly connected
 */

import { Network } from './types';

/**
 * Check if two stations are directly connected
 * @param network - Network adjacency list
 * @param start - Start station
 * @param end - End station
 * @returns True if direct connection exists
 */
export function hasDirectRoute(network: Network, start: string, end: string): boolean {
  // TODO: Implement using TDD
  throw new Error('Not implemented yet - write your tests first!');
}

/**
 * Algorithm 5: Calculate Total Stops
 * Count the number of stops in a route
 */

import { Network } from './types';

/**
 * Calculate number of stops in a route
 * @param network - Network adjacency list
 * @param route - Array of stations in route
 * @returns Number of stops, or -1 if route is invalid
 */
export function countStops(network: Network, route: string[]): number {
  // TODO: Implement using TDD
  // Validate each connection exists
  throw new Error('Not implemented yet - write your tests first!');
}

/**
 * Algorithm 7: Calculate Route Duration
 * Calculate total travel time for a route
 */

import { WeightedNetwork } from './types';

/**
 * Calculate total travel time for a route
 * @param weightedNetwork - Weighted network adjacency list
 * @param route - Array of stations in route
 * @returns Total travel time in minutes, or -1 if invalid
 */
export function calculateDuration(
  weightedNetwork: WeightedNetwork,
  route: string[]
): number {
  // TODO: Implement using TDD
  // Sum up all travel times between consecutive stations
  throw new Error('Not implemented yet - write your tests first!');
}

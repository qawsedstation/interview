/**
 * Requirement 1: Basic Route Display
 * Parse and display all direct train connections
 */

import { Route } from '../types';
import { routes, getStationName } from '../data/eurostarRoutes';

/**
 * Display all direct routes in format: "Origin → Destination"
 * @param routeData - Array of routes to display
 * @returns Array of formatted route strings
 */
export function displayDirectRoutes(routeData: Route[] = routes): string[] {
  // TODO: Implement this function using TDD
  // Remember: Write tests first, then make them pass!
  throw new Error('Not implemented yet - start by writing tests!');
}
